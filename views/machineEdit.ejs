<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <link rel='stylesheet' href='/stylesheets/style.css' />
    <title></title>
    <script type="text/javascript">
    function Check() {

        var name = document.getElementsByName('name')[0].value;
        var storeid = document.getElementsByName('storeid')[0].value;
        
        if (name && storeid) {
            return true;
        }
        alert('please input the info.');
        return false;

    }
    </script>
</head>

<body>
    <form name="editform" action="machineEdit" method="post" accept-charset="utf-8" onSubmit="return Check();">
        <h1>機台 - 修改</h1>
        <input type="hidden" name="id" value="<%=data[0].id  %>" />
        <div class="row">
            <label>名稱:</label>
            <input type="text" name="name" placeholder="name" value="<%=data[0].name %>"  />
        </div>
        <div class="row">
            <label>店舖:</label>            
            <select name="storeid">
                <% for ( var i = 0 ; i < store.length ; i++){ %>
                <option value="<%= store[i].id  %>" <%= data[0].storeid == store[i].id ? "selected='true'":""%>><%= store[i].name  %></option>                
                <% } %>
            </select>
        </div>   
        <div class="row">
            <input class="btn" type='submit' value='提交'>
            <input class="btn" type='reset' value='重置'>
            <input class="btn" type='button' value='上一頁' onclick="javascript: window.history.back();">
        </div>
    </form>
</body>

</html>
